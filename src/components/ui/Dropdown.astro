---
const {title, textSize} = Astro.props;

const currentPath = Astro.url.pathname;
//console.log("Desde Dropdown", currentPath)

---
<div>
    <p class:list={[`drop-menu-trigger a-link text-center cursor-pointer ${textSize} transition-all ease-in-out sm:text-base`,
        {"text-ocre-110": currentPath.includes(title.toLowerCase())},
        /* {"text-ocreNeutral-120": currentPath.includes(title)}, */
    ]}>{title} ▾</p>
    <slot ></slot>
    <!-- <ul class="drop-menu mt-2 space-y-1 absolute hidden
        bg-white rounded-xl top-9">
        {optionsList.map((item:any) => (
            <li class="flex flex-col items-start">
                <a 
                    href={`${import.meta.env.BASE_URL}${title.toLowerCase()}/${item.path}`} 
                    class="block text-sm text-ocreNeutral-120 hover:text-ocre-110 px-4 py-2"
                >
                    {item.title}
                </a>
            </li>
        ))}
    </ul> -->
</div>
